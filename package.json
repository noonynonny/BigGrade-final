{
  "name": "biggrade-netlify-proxy",
  "version": "1.0.0",
  "description": "Netlify proxy for BigGrade Base44 app",
  "main": "index.js",
  "scripts": {
    "build": "echo 'No build needed - using serverless functions'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "node-fetch": "^2.6.7"
  },
  "author": "",
  "license": "MIT"
}
